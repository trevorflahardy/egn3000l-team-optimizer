[project]
name = "egn3000l-team-optimizer"
version = "1.0.0"
description = "Optimal team formation for EGN3000L using survey data, Numba-accelerated local search, and multi-objective scoring."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

dependencies = ["numpy=2.3.5", "numba>=0.58"]
